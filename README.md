# Decentralized_Chat-
A peer-to-peer encrypted messaging app. Each user runs their own FastAPI "node." Nodes talk directly to each other over HTTP/WebSocket, with end-to-end encryption via PyNaCl — no central server holds messages or keys.
